export default function OfflinePage() {
  return <main style={{ padding: 32, maxWidth: 640, margin: "0 auto" }}><h1>Admin is offline</h1><p>The navigation shell remains available, but administration changes require a live, verified connection.</p><a href="/">Retry admin home</a></main>;
}
