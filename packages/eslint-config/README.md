# ESLint Config

Shared linting presets live here.

Apps and packages should extend these configs instead of defining separate linting rules locally. Import restrictions can be tightened here as source files are added.
