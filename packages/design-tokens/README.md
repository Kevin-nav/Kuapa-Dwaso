# Design Tokens

Owns shared visual constants such as colors, spacing, typography, radii, shadows, breakpoints, z-index values, and status colors.

Apps should not invent reusable visual constants locally.

