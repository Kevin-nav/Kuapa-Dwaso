# Dashboard UI

Owns heavier operational components for authenticated and admin dashboards.

The public site must not import this package.

`PilotTransactionSummary` composes the lightweight pilot UI primitives for
authenticated operational and administrative pages.
