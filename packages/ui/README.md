# UI

Owns shared everyday components used across apps.

Examples include buttons, inputs, selects, modals, badges, tabs, toasts, tooltips, form fields, empty states, loading states, error states, and page headers.

