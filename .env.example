# Convex
# `corepack pnpm convex:dev` writes these values to the root `.env.local`.
CONVEX_DEPLOYMENT=
CONVEX_URL=
CONVEX_SITE_URL=

# Next.js client apps also need this browser-exposed copy of `CONVEX_URL`
# in their app-level env files.
NEXT_PUBLIC_CONVEX_URL=
